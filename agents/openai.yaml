interface:
  display_name: "Organic Discovery Operator"
  short_description: "Audit and improve SEO, AEO, GEO, AI citations, and qualified organic growth."
  brand_color: "#111827"
  default_prompt: "Run the deterministic audit when a page is available, establish Business Truth, map demand and the AI shelf, fix the earliest blocker, prepare approved owned and earned work, measure the business result, and preserve rollback."
policy:
  allow_implicit_invocation: true
