# v0.5.0 validation repair

The first post-merge release check exposed contract drift in `scripts/validate_skill.py`.

The validator had asserted output fields that the shipped v0.5 fact, shelf, and wedge tools do not emit, while several versioned documentation and evaluation files still described v0.4. The repair:

- validates the actual v0.4 audit and v0.5 fact, observation, shelf, and wedge contracts;
- preserves branded-query exclusion, exact-surface grouping, rate denominators, publication gates, and rejected-wedge reasons;
- aligns roadmap, definition of done, and trigger-eval metadata with v0.5.0;
- removes temporary one-shot release workflows.

The original failed workflow remains in GitHub history. The repaired default-branch commit is accepted only after the normal Python 3.11 and 3.13 workflow passes.
